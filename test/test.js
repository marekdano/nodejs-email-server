'use strict';
var assert = require('assert');
var serverjs = require('../');

describe('serverjs node module', function () {
  it('must have at least one test', function () {
    serverjs();
    assert(false, 'Not any test yet.');
  });
});
